import { Component, OnInit } from '@angular/core';

/**
 * Componente para la pagina de la agenda del abogado
 */
@Component({
    selector: 'app-agenda-abogado',
    templateUrl: './agenda-abogado.component.html'
})
export class AgendaAbogadoComponent implements OnInit {

    /**
     * PostConstructor que permite inicializar las variables del component
     */
    ngOnInit(): void { }
}